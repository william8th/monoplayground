include 'aurora',
        'ktor',
        'coroutines',
        'cucumber',
        'hibernate',
        'jdbi-bean',
        'graphql',
        'codility',
        'resilience4j'

rootProject.name = 'monoplayground'
rootProject.children.each { child ->
    child.name = child.name - ~/.*\\//
    child.buildFileName = "${child.name}.gradle"
    print(child)
}
